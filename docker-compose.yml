services:
  postgres:
    image: postgres:16
    container_name: cleanarch-postgres
    environment:
      POSTGRES_DB: cleanarch
      POSTGRES_USER: cleanarch
      POSTGRES_PASSWORD: cleanarch
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # app:
  #   build: .
  #   container_name: cleanarch-app
  #   depends_on:
  #     - postgres
  #   environment:
  #     SPRING_PROFILES_ACTIVE: dev
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/cleanarch
  #     SPRING_DATASOURCE_USERNAME: cleanarch
  #     SPRING_DATASOURCE_PASSWORD: cleanarch
  #   ports:
  #     - "8080:8080"

volumes:
  postgres_data:
